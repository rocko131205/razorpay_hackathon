# The reconciliation engine itself (recon/) needs nothing beyond the standard
# library — `python run_recon.py` works on a bare interpreter. Everything below
# is for the dashboard and the two optional integrations.

streamlit>=1.34        # dashboard
requests>=2.28         # Razorpay test-mode API
anthropic>=0.40        # settlement Q&A; the app degrades to a ledger-only
                       # answer when ANTHROPIC_API_KEY is unset
pytest>=8.0            # tests
